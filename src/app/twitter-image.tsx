/**
 * Twitter/X card image. The Open Graph card is already sized 1200x630 for
 * `summary_large_image`, so reuse it rather than maintaining a second design.
 */
export { default, alt, size, contentType } from "./opengraph-image";
